#include 'template.wml'

<p>
<tt>||</tt> and <tt>&amp;&amp;</tt> return the last argument that was
evaluated. Thus, <tt>||</tt> is useful for assigning default values,
like this:
</p>

<quadpres_code_block>
<quadpres_include_colorized_file filename="src/and_or/shift-or.pl" />
</quadpres_code_block>

<qpcontents />

