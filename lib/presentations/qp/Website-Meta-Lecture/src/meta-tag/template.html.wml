#include 'template.wml'

<quadpres_code_file filename="src/examples/meta-tag/template.wml" />

<h2>Explanation</h2>

<ul>
<li>
<code>&lt;if ... /&gt;</code> is a conditional. It evaluates to the second
argument if its condition is not an empty space, and to the third otherwise.
</li>
<li>
<code>%0</code> and <code>%1</code> are the arguments to the tag.
</li>
<li>
<code>;&#059;;</code> is a comment of mp4h.
</li>
</ul>
