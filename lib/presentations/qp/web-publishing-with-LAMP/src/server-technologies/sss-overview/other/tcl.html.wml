#include 'template.wml'

<points>
<li>
<a href="http://www.tcl.tk/">Tcl</a> is a language that used to compete
for the same niche as Perl, with the advantage of having a GUI toolkit (Tk).
</li>
<li>
Has some serious limitations. (C-like NULL-terminated strings, strings as
closures).
</li>
<li>
Not commonly used for web-scripting, except mostly as part of the
<a href="http://openacs.org/">OpenACS framework</a>.
</li>
</points>

