#include 'template.wml'

<p>
There are two special code blocks for perl modules - <code>BEGIN</code> and
<code>END</code>. These blocks are executed when a module is first loaded,
and when the perl interpreter is about to unload it, respectively.
</p>

<p>
Here's an example for a logging module that makes use of this facility:
</p>

<quadpres_code_file filename="src/modules/MyLog.pm" />
