#include 'template.wml'

<points>
<li>
The configuration tools in Red Hat start with <tt>redhat-config</tt>.
</li>
<li>
You can get a list of them by starting a command prompt, typing
<tt>redhat-config</tt> and press the <tt>TAB</tt> key several times.
</li>
</points>

