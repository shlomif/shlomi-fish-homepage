#!/bin/bash
rebuild()
{
    (
        set -x
        set -e
        for d in ../_*--clones/*
        do
            ( cd "$d"; git clean -dxf . )
        done
        git clean -dxf .
        bash -x bin/install-npm-deps.sh
        ./gen-helpers
        make -j4 fastrender
        make -j4 test
    )
}
rebuild
