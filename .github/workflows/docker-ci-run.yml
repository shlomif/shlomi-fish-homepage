# This file is GENERATED BY
# bin/CI-testing/translate-travis.yml-to-github-actions.py
jobs:
    test-docker-ci-run:
        runs-on: ubuntu-latest
        steps:
        -   uses: actions/checkout@v2
        -   run: sudo apt-get update -qq
        -   run: sudo apt-get -y remove libgd3 libunwind-14 nginx
        -   run: sudo apt-get --no-install-recommends install -y eatmydata
        -   run: sudo eatmydata apt-get --no-install-recommends install -y build-essential
                cpanminus libperl-dev libxml2-dev libxslt1-dev perl xz-utils zip
        -   run: sudo cpanm --verbose Carp Carp::Always Docker::CLI::Wrapper::Container
                Moo Path::Tiny
        -   run: perl bin/docker-ci-run.pl --regex-filter='fedora'
        timeout-minutes: 60
name: test-docker-ci-run
'on':
- push
