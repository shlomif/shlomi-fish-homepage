#include 'template.wml'

<quadpres_code_block>
<quadpres_include_colorized_file filename="src/examples/frames/Makefile" />
</quadpres_code_block>

<h2>Explanation</h2>

<p>
From each file we generate two files. One with a regular extension in which the
slice <code>NOFRAMES</code> is defined. The other with a <code>.frames.html</code>
extension with the slice <code>FRAMES</code> defined.
</p>


