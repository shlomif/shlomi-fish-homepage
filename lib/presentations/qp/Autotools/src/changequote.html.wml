#include 'template.wml'

<points>

<li>
The m4 preprocessor which is used to process <code>configure.in</code>
uses <code>[</code> and <code>]</code> for grouping arguments.
</li>

<li>
If you want to use them yourself, type <code>changequote(, )</code> at the
beginning of the offending code and <code>changequote([, ])</code> afterwards.
</li>

<li>
Here's an example:
</li>

</points>

<quadpres_code_file filename="src/changequote/configure.in" />
