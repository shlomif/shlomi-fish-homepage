#include 'template.wml'


<ul class="point">
<li>
Start from a prepared lecture (like this one) by unpacking its archive,
or <code>cp -r</code>'ing it.
</li>
<li>
In the file <code>Render_all_contents.pl</code> change the
<code>default_dest_dir</code> in the default target to the path you wish
to put the static HTML in.
</li>
</ul>
