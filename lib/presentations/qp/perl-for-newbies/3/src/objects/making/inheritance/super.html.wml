#include 'template.wml'

<p>
It is possible to explicitly call the method of a base class even if it
was overridden by another method in the derived class. To do that use
the <code>SUPER::</code> prefix before the method name.
</p>

<p>
Here is a rewrite of the <code>Bar</code> class, this time with
<code>assign_name</code> retaining its name.
</p>

<quadpres_code_block>
<quadpres_include_colorized_file filename="src/objects/making/inheritance/Bar2.pm" />
</quadpres_code_block>
