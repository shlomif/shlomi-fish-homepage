#include 'template.wml'

<p>
To call the methods of an object, one should use the
<code>$object_ref-&gt;method_name(@args)</code> notation.
</p>

<p>
Note that internally the method receives <code>$object_ref</code>
as its first argument.
</p>
