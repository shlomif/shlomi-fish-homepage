#include 'template.wml'

<p>
The statement <code>die</code> throws an exception which can be any Perl
scalar. The statement <code>eval { ... }</code> catches an excpetion that was
given inside it, and after it sets the special variable <code>$@</code>
to be the value of the exception or undef if none was caught.
</p>

<p>
Here's an example:
</p>

<quadpres_code_block>
<quadpres_include_colorized_file filename="src/exceptions/die-eval.pl" />
</quadpres_code_block>

