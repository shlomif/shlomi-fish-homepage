#include 'template.wml'

<p>
By default, the regular expression repetition operators such as
<code>*</code>, <code>+</code> and friends are greedy. Hence, they will try to
match as much as possible of the string as they can.
</p>

<p>
If you wish them to match as little as possible, append a question mark
(<code>?</code>) after them. Here's an example:
</p>

<quadpres_code_file filename="src/regexps/substitute/ungreedy.pl" />
