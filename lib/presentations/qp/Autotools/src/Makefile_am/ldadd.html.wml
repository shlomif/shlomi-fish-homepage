#include 'template.wml'


<points>

<li>
To link a target with an internal library specify the target name as before
along with <code>_LDADD</code> variable and type the library name ending
with <code>.la</code>.
</li>

</points>
