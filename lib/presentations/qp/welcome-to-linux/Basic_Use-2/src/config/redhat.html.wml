#include 'template.wml'

<points>
<li>
The configuration tools in Red Hat start with <code>redhat-config</code>.
</li>
<li>
You can get a list of them by starting a command prompt, typing
<code>redhat-config</code> and press the <code>TAB</code> key several times.
</li>
</points>
