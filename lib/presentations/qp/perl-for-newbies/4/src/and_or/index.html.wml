#include 'template.wml'

<p>
<code>||</code> and <code>&amp;&amp;</code> return the last argument that was
evaluated. Thus, <code>||</code> is useful for assigning default values,
like this:
</p>

<quadpres_code_file filename="src/and_or/shift-or.pl" />

<qpcontents />
