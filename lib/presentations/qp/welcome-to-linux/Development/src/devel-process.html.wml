#include 'template.wml'

<points>
<li>
Development on Linux takes place by editing text files, running command-line
commands to build them into executables (from the command line), and invoking
them.
</li>
<li>
There is often a debug stage using an interactive debugger.
</li>
<li>
Afterwards, one can modify the files and build again.
</li>
</points>
