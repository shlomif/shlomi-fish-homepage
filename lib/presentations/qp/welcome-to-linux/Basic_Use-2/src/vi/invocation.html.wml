#include 'template.wml'

<points>

<li>
To invoke vi, once in a shell, enter the command
<code>vi [filename]</code> where <code>[filename]</code> is the name
of the file you wish to edit.
</li>
</points>
